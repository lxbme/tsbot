# Phase 2B Implementation Plan：播放列表

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 命名歌单的本地 TOML 持久化：从当前队列保存、增量增删、列出/查看/加载(追加播放)/删除，通过 `!playlist <子命令>` 操作。

**Architecture:** 新增 `crates/playlist`（Store + 文件格式，独立可测）。`source::Resolved` 增 `request`（原始输入），`player` 的 `NowPlaying`/`QueueItem` 携带 `request`，使保存当前队列能取原始请求。`commands` 加 `!playlist` 解析与 `handle_playlist` 分发，`run` 增 `Store` 参数。`config` 加 `[playlist] dir`。

**Tech Stack:** 现有 workspace + serde/toml（歌单文件）。

**前置:** Phase 2A 已合并入 `main`（21 测试 + Ctrl+C 优雅退出修复）。

---

## 已核验
- `toml` 把 `Vec<struct>` 序列化为 `[[items]]` 数组表，往返正常（标准行为）。
- `config` 的 `[playlist]` 用 `#[serde(default)]` 使旧配置（无该段）仍可加载。
- 触点：`source::resolve` 三处构造 `Resolved`；`player` 的 `NowPlaying`(行61)/`QueueItem`(行68)/`update_snapshot`(行148)；`commands::run`(行139) 签名 + bin `main.rs:36` 调用；config `Config`(行7)。

## File Structure（2B 后）
```
crates/playlist/Cargo.toml, src/lib.rs   PlaylistItem/Playlist/Store/valid_name + tests
crates/source/src/lib.rs                 Resolved + request
crates/player/src/lib.rs                 NowPlaying/QueueItem + request
crates/config/src/lib.rs                 Config + [playlist] dir
crates/commands/src/lib.rs, Cargo.toml   Command::Playlist + handle_playlist + run(Store)
crates/tsbot/src/main.rs, Cargo.toml     构造 Store 传入 run
config.example.toml, .gitignore          [playlist] 段；忽略 /playlists/
```

---

## Task 1: crates/playlist 子包

**Files:** Create `crates/playlist/Cargo.toml`, `crates/playlist/src/lib.rs`; Modify root `Cargo.toml`

- [ ] **Step 1: 写 crates/playlist/Cargo.toml**

```toml
[package]
name = "tsbot-playlist"
version = "0.1.0"
edition = "2021"

[lib]
name = "playlist"

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
```

- [ ] **Step 2: 写 crates/playlist/src/lib.rs（实现 + 测试，VERBATIM）**

```rust
use std::path::PathBuf;

use anyhow::{bail, Context, Result};
use serde::{Deserialize, Serialize};

#[derive(Clone, Serialize, Deserialize)]
pub struct PlaylistItem {
    pub request: String,
    pub title: String,
}

#[derive(Clone, Serialize, Deserialize)]
pub struct Playlist {
    pub name: String,
    pub items: Vec<PlaylistItem>,
}

/// 歌单名校验：非空，且每字符为 字母数字(含 Unicode) / '-' / '_'。
/// 拒绝空、含 '/'、'.'、空格、'..' 等（防路径穿越，安全关键）。
pub fn valid_name(name: &str) -> bool {
    !name.is_empty() && name.chars().all(|c| c.is_alphanumeric() || c == '-' || c == '_')
}

/// 歌单存储：每个歌单一个 <dir>/<name>.toml。
pub struct Store {
    dir: PathBuf,
}

impl Store {
    pub fn new(dir: PathBuf) -> Self {
        Self { dir }
    }

    fn path_for(&self, name: &str) -> Result<PathBuf> {
        if !valid_name(name) {
            bail!("歌单名只能含字母数字、- 和 _");
        }
        Ok(self.dir.join(format!("{name}.toml")))
    }

    pub fn save(&self, p: &Playlist) -> Result<()> {
        let path = self.path_for(&p.name)?;
        std::fs::create_dir_all(&self.dir).context("创建歌单目录失败")?;
        let text = toml::to_string(p).context("序列化歌单失败")?;
        std::fs::write(&path, text).context("写入歌单失败")?;
        Ok(())
    }

    pub fn load(&self, name: &str) -> Result<Playlist> {
        let path = self.path_for(name)?;
        if !path.exists() {
            bail!("歌单 {name} 不存在");
        }
        let text = std::fs::read_to_string(&path).context("读取歌单失败")?;
        toml::from_str(&text).context("解析歌单失败")
    }

    pub fn list(&self) -> Result<Vec<String>> {
        let mut names = Vec::new();
        let rd = match std::fs::read_dir(&self.dir) {
            Ok(rd) => rd,
            Err(_) => return Ok(names), // 目录不存在 → 空列表
        };
        for entry in rd.flatten() {
            let p = entry.path();
            if p.extension().and_then(|s| s.to_str()) == Some("toml") {
                if let Some(stem) = p.file_stem().and_then(|s| s.to_str()) {
                    names.push(stem.to_string());
                }
            }
        }
        names.sort();
        Ok(names)
    }

    pub fn delete(&self, name: &str) -> Result<()> {
        let path = self.path_for(name)?;
        if !path.exists() {
            bail!("歌单 {name} 不存在");
        }
        std::fs::remove_file(&path).context("删除歌单失败")?;
        Ok(())
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn store() -> (tempfile::TempDir, Store) {
        let dir = tempfile::tempdir().unwrap();
        let s = Store::new(dir.path().to_path_buf());
        (dir, s)
    }

    fn pl(name: &str, items: &[(&str, &str)]) -> Playlist {
        Playlist {
            name: name.to_string(),
            items: items
                .iter()
                .map(|(r, t)| PlaylistItem { request: r.to_string(), title: t.to_string() })
                .collect(),
        }
    }

    #[test]
    fn valid_name_rules() {
        assert!(valid_name("my_list-1"));
        assert!(valid_name("歌单"));
        assert!(!valid_name(""));
        assert!(!valid_name("a/b"));
        assert!(!valid_name(".."));
        assert!(!valid_name("a b"));
        assert!(!valid_name("a.toml"));
    }

    #[test]
    fn save_then_load_roundtrip() {
        let (_d, s) = store();
        let p = pl("mix", &[("https://x", "Song X"), ("/a.mp3", "Local A")]);
        s.save(&p).unwrap();
        let got = s.load("mix").unwrap();
        assert_eq!(got.name, "mix");
        assert_eq!(got.items.len(), 2);
        assert_eq!(got.items[0].request, "https://x");
        assert_eq!(got.items[0].title, "Song X");
        assert_eq!(got.items[1].title, "Local A");
    }

    #[test]
    fn list_sorted_and_delete() {
        let (_d, s) = store();
        s.save(&pl("beta", &[])).unwrap();
        s.save(&pl("alpha", &[])).unwrap();
        assert_eq!(s.list().unwrap(), vec!["alpha".to_string(), "beta".to_string()]);
        s.delete("alpha").unwrap();
        assert_eq!(s.list().unwrap(), vec!["beta".to_string()]);
    }

    #[test]
    fn load_missing_and_bad_name_error() {
        let (_d, s) = store();
        assert!(s.load("nope").is_err());
        assert!(s.save(&pl("a/b", &[])).is_err());
        assert!(s.delete("..").is_err());
    }

    #[test]
    fn list_empty_dir_ok() {
        let (_d, s) = store();
        assert!(s.list().unwrap().is_empty());
    }
}
```

- [ ] **Step 3: 加入 workspace members**

根 `Cargo.toml` members 追加 `crates/playlist`（置于 source 之后）：
```toml
members = ["crates/config", "crates/audio", "crates/ts-connection", "crates/source", "crates/playlist", "crates/player", "crates/commands", "crates/tsbot"]
```

- [ ] **Step 4: 测试**

Run: `cargo test -p tsbot-playlist 2>&1 | grep "test result"`
Expected: 5 测试 PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(playlist): 歌单存储 Store + TOML 格式 + 名字校验"
```

---

## Task 2: source + player 增加 request 字段

**Files:** Modify `crates/source/src/lib.rs`, `crates/player/src/lib.rs`, `crates/commands/src/lib.rs`

- [ ] **Step 1: source — Resolved 加 request**

在 `crates/source/src/lib.rs`：把 `Resolved` 改为：
```rust
pub struct Resolved {
    pub input: String,
    pub title: String,
    pub duration: Option<Duration>,
    pub request: String,
}
```
在 `resolve` 的本地分支：
```rust
        return Ok(Resolved { input: arg.to_string(), title, duration, request: arg.to_string() });
```
http 回退分支：
```rust
            Err(_) => Ok(Resolved { input: arg.to_string(), title: arg.to_string(), duration: None, request: arg.to_string() }),
```
在 `ytdlp_meta(url)` 末尾的 `Ok(Resolved { input, title, duration })` 改为：
```rust
    Ok(Resolved { input, title, duration, request: url.to_string() })
```

- [ ] **Step 2: player — NowPlaying/QueueItem 加 request，update_snapshot 填充**

在 `crates/player/src/lib.rs`：
```rust
#[derive(Clone)]
pub struct NowPlaying {
    pub title: String,
    pub elapsed: Duration,
    pub duration: Option<Duration>,
    pub request: String,
}

#[derive(Clone)]
pub struct QueueItem {
    pub title: String,
    pub duration: Option<Duration>,
    pub request: String,
}
```
在 `update_snapshot` 中，now_playing 映射加 `request`，upcoming 映射加 `request`：
```rust
        s.now_playing = self.current.as_ref().map(|c| NowPlaying {
            title: c.resolved.title.clone(),
            elapsed: Duration::from_millis(c.played_frames * FRAME_MS),
            duration: c.resolved.duration,
            request: c.resolved.request.clone(),
        });
        s.upcoming = self
            .queue
            .iter()
            .map(|r| QueueItem { title: r.title.clone(), duration: r.duration, request: r.request.clone() })
            .collect();
```
在 player 的测试 helper `res` 加 request：
```rust
    fn res(title: &str) -> Resolved {
        Resolved { input: format!("/nonexistent/{title}"), title: title.to_string(), duration: None, request: title.to_string() }
    }
```

- [ ] **Step 3: commands 测试编译修复（NowPlaying/QueueItem 多了字段）**

在 `crates/commands/src/lib.rs` 的 `format_nowplaying_and_queue` 测试里，构造 `NowPlaying`/`QueueItem` 处补 `request`：
```rust
        s.now_playing = Some(NowPlaying {
            title: "Song".into(),
            elapsed: Duration::from_secs(83),
            duration: Some(Duration::from_secs(245)),
            request: "req".into(),
        });
        s.upcoming = vec![QueueItem { title: "Next".into(), duration: Some(Duration::from_secs(190)), request: "req2".into() }];
```

- [ ] **Step 4: 构建 + 全测试**

Run: `cargo build --workspace 2>&1 | tail -5 && cargo test --workspace 2>&1 | grep -E "test result" | grep -oE "[0-9]+ passed" | awk '{s+=$1} END {print s" passed"}'`
Expected: 编译成功；全绿（仍 26 含 playlist 5 = 26）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(source,player): Resolved/NowPlaying/QueueItem 增加 request（原始请求）"
```

---

## Task 3: config 加 [playlist] dir

**Files:** Modify `crates/config/src/lib.rs`, `config.example.toml`, `.gitignore`

- [ ] **Step 1: Config 增 playlist 段**

在 `crates/config/src/lib.rs` 的 `Config` 增字段，并加 `PlaylistCfg`：
```rust
#[derive(Debug, Deserialize)]
pub struct Config {
    pub server: Server,
    pub bot: Bot,
    #[serde(default)]
    pub playlist: PlaylistCfg,
}

#[derive(Debug, Deserialize)]
pub struct PlaylistCfg {
    #[serde(default = "default_playlist_dir")]
    pub dir: String,
}

impl Default for PlaylistCfg {
    fn default() -> Self {
        Self { dir: default_playlist_dir() }
    }
}

fn default_playlist_dir() -> String {
    "playlists".to_string()
}
```
（`Server`/`Bot`/`load` 不变。）

- [ ] **Step 2: 加测试（默认 + 覆盖）**

在 config 测试模块追加（沿用现有 `write_tmp(body) -> (TempDir, PathBuf)` helper）：
```rust
    #[test]
    fn playlist_dir_defaults_and_override() {
        let (_d, p) = write_tmp(
            "[server]\naddress = \"x\"\n[bot]\nname = \"b\"\nidentity_path = \"i\"\n",
        );
        assert_eq!(load(&p).unwrap().playlist.dir, "playlists");

        let (_d2, p2) = write_tmp(
            "[server]\naddress = \"x\"\n[bot]\nname = \"b\"\nidentity_path = \"i\"\n[playlist]\ndir = \"/tmp/pl\"\n",
        );
        assert_eq!(load(&p2).unwrap().playlist.dir, "/tmp/pl");
    }
```

- [ ] **Step 3: 更新 config.example.toml（追加 [playlist] 段）**

在 `config.example.toml` 末尾追加：
```toml

[playlist]
dir = "playlists"       # 歌单 TOML 存放目录（默认 playlists）
```

- [ ] **Step 4: .gitignore 忽略歌单目录**

在 `.gitignore` 追加一行（保留已有项）：
```
/playlists/
```

- [ ] **Step 5: 测试**

Run: `cargo test -p tsbot-config 2>&1 | grep "test result"`
Expected: 3 测试 PASS（原 2 + 新增 1）。

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(config): 增加 [playlist] dir（serde default 兼容旧配置）"
```

---

## Task 4: commands 的 !playlist 命令 + bin 接线

**Files:** Modify `crates/commands/Cargo.toml`, `crates/commands/src/lib.rs`, `crates/tsbot/Cargo.toml`, `crates/tsbot/src/main.rs`

- [ ] **Step 1: commands 依赖加 tsbot-playlist + dev tempfile**

`crates/commands/Cargo.toml`：`[dependencies]` 增 `tsbot-playlist = { path = "../playlist" }`；若无 `[dev-dependencies]` 段则新增：
```toml
[dev-dependencies]
tempfile = { workspace = true }
```

- [ ] **Step 2: commands lib —— imports + Command 变体 + parse + help**

在 `crates/commands/src/lib.rs` 顶部 `use` 增加：
```rust
use playlist::{Playlist, PlaylistItem, Store};
```
`Command` 枚举增加变体：
```rust
    Playlist(Vec<String>),
```
`parse` 的 match 增加分支（在 `_ => None` 之前）：
```rust
        "playlist" => Some(Command::Playlist(arg.split_whitespace().map(|s| s.to_string()).collect())),
```
`HELP_TEXT` 末尾追加一行（在现有字符串里加）：
```
!playlist save|create|add|remove|list|view|play|delete <名> 歌单
```
具体：把 `HELP_TEXT` 常量最后的 `!help 帮助";` 改为：
```rust
!help 帮助\n\
!playlist <save|create|add|remove|list|view|play|delete> 歌单";
```

- [ ] **Step 3: commands lib —— handle_command 增 store 参数 + 新增 handle_playlist**

把 `run` 与 `handle_command` 改为接收 `Store`，并新增 `handle_playlist` 与 `playlist_from_snapshot`：

`run` 改为：
```rust
pub async fn run(
    mut chat_rx: mpsc::Receiver<ChatMessage>,
    handle: PlayerHandle,
    snapshot: Arc<Mutex<Snapshot>>,
    store: Store,
    reply_tx: mpsc::Sender<String>,
) {
    while let Some(msg) = chat_rx.recv().await {
        let Some(cmd) = parse(&msg.text) else { continue };
        let reply = handle_command(cmd, &handle, &snapshot, &store).await;
        let _ = reply_tx.send(reply).await;
    }
}
```
`handle_command` 签名加 `store: &Store`，并在其 match 末尾（`Command::Remove` 之后）加一臂：
```rust
        Command::Playlist(tokens) => handle_playlist(&tokens, store, handle, snapshot).await,
```
即把 `async fn handle_command(cmd: Command, handle: &PlayerHandle, snapshot: &Arc<Mutex<Snapshot>>) -> String {` 改为 `async fn handle_command(cmd: Command, handle: &PlayerHandle, snapshot: &Arc<Mutex<Snapshot>>, store: &Store) -> String {`。

在 `handle_command` 之后新增：
```rust
fn playlist_from_snapshot(name: &str, s: &Snapshot) -> Playlist {
    let mut items = Vec::new();
    if let Some(np) = &s.now_playing {
        items.push(PlaylistItem { request: np.request.clone(), title: np.title.clone() });
    }
    for q in &s.upcoming {
        items.push(PlaylistItem { request: q.request.clone(), title: q.title.clone() });
    }
    Playlist { name: name.to_string(), items }
}

async fn handle_playlist(
    tokens: &[String],
    store: &Store,
    handle: &PlayerHandle,
    snapshot: &Arc<Mutex<Snapshot>>,
) -> String {
    let sub = tokens.first().map(|s| s.as_str()).unwrap_or("");
    match sub {
        "list" => match store.list() {
            Ok(names) if names.is_empty() => "还没有歌单".to_string(),
            Ok(names) => format!("歌单：{}", names.join("、")),
            Err(e) => format!("出错：{e}"),
        },
        "save" => {
            let Some(name) = tokens.get(1) else { return "用法：!playlist save <名>".to_string() };
            let p = {
                let s = snapshot.lock().unwrap();
                playlist_from_snapshot(name, &s)
            };
            let n = p.items.len();
            match store.save(&p) {
                Ok(()) => format!("已保存歌单 [b]{name}[/b]（{n} 首）"),
                Err(e) => format!("保存失败：{e}"),
            }
        }
        "create" => {
            let Some(name) = tokens.get(1) else { return "用法：!playlist create <名>".to_string() };
            if store.load(name).is_ok() {
                return format!("歌单 {name} 已存在");
            }
            let p = Playlist { name: name.to_string(), items: Vec::new() };
            match store.save(&p) {
                Ok(()) => format!("已创建空歌单 [b]{name}[/b]"),
                Err(e) => format!("创建失败：{e}"),
            }
        }
        "delete" => {
            let Some(name) = tokens.get(1) else { return "用法：!playlist delete <名>".to_string() };
            match store.delete(name) {
                Ok(()) => format!("已删除歌单 [b]{name}[/b]"),
                Err(e) => e.to_string(),
            }
        }
        "view" => {
            let Some(name) = tokens.get(1) else { return "用法：!playlist view <名>".to_string() };
            match store.load(name) {
                Ok(p) if p.items.is_empty() => format!("歌单 [b]{name}[/b] 为空"),
                Ok(p) => {
                    let mut out = format!("歌单 [b]{name}[/b]（{} 首）", p.items.len());
                    for (i, it) in p.items.iter().enumerate() {
                        out.push_str(&format!("\n{}. {}", i + 1, it.title));
                    }
                    out
                }
                Err(e) => e.to_string(),
            }
        }
        "remove" => {
            let (Some(name), Some(ns)) = (tokens.get(1), tokens.get(2)) else {
                return "用法：!playlist remove <名> <编号>".to_string();
            };
            let Ok(n) = ns.parse::<usize>() else { return "编号需为正整数".to_string() };
            let mut p = match store.load(name) {
                Ok(p) => p,
                Err(e) => return e.to_string(),
            };
            if n < 1 || n > p.items.len() {
                return format!("歌单里没有第 {n} 首");
            }
            let removed = p.items.remove(n - 1);
            match store.save(&p) {
                Ok(()) => format!("已从 {name} 移除 [b]{}[/b]", removed.title),
                Err(e) => format!("保存失败：{e}"),
            }
        }
        "add" => {
            let (Some(name), Some(raw)) = (tokens.get(1), tokens.get(2)) else {
                return "用法：!playlist add <名> <url>".to_string();
            };
            let url = strip_url_bbcode(raw);
            let title = match source::resolve(&url).await {
                Ok(r) => r.title,
                Err(e) => return format!("解析失败：{e}"),
            };
            let mut p = store
                .load(name)
                .unwrap_or_else(|_| Playlist { name: name.to_string(), items: Vec::new() });
            p.items.push(PlaylistItem { request: url, title: title.clone() });
            match store.save(&p) {
                Ok(()) => format!("已加入 [b]{title}[/b] → 歌单 {name}"),
                Err(e) => format!("保存失败：{e}"),
            }
        }
        "play" => {
            let Some(name) = tokens.get(1) else { return "用法：!playlist play <名>".to_string() };
            let p = match store.load(name) {
                Ok(p) => p,
                Err(e) => return e.to_string(),
            };
            let mut ok = 0usize;
            let mut fail = 0usize;
            for it in &p.items {
                match source::resolve(&it.request).await {
                    Ok(r) => {
                        handle.play(r);
                        ok += 1;
                    }
                    Err(_) => fail += 1,
                }
            }
            if fail > 0 {
                format!("已加载歌单 [b]{name}[/b]（成功 {ok}，失败 {fail}）")
            } else {
                format!("已加载歌单 [b]{name}[/b]（{ok} 首）")
            }
        }
        _ => "用法：!playlist <save|create|add|remove|list|view|play|delete>".to_string(),
    }
}
```

- [ ] **Step 4: 更新 run 的现有测试 + 新增 playlist 测试**

`crates/commands/src/lib.rs` 的 `run_volume_loop_remove_replies` 测试现在调用 `run(...)` 缺 `store` 参数。把该测试中：
```rust
        let join = tokio::spawn(run(chat_rx, handle, snap, reply_tx));
```
改为（先建临时 Store）：
```rust
        let dir = tempfile::tempdir().unwrap();
        let store = Store::new(dir.path().to_path_buf());
        let join = tokio::spawn(run(chat_rx, handle, snap, store, reply_tx));
```

并在测试模块追加 playlist 解析与 handle_playlist 测试：
```rust
    fn toks(s: &str) -> Vec<String> {
        s.split_whitespace().map(|x| x.to_string()).collect()
    }

    #[test]
    fn parse_playlist_tokens() {
        assert!(matches!(parse("!playlist save mix"), Some(Command::Playlist(v)) if v == vec!["save","mix"]));
        assert!(matches!(parse("!playlist"), Some(Command::Playlist(v)) if v.is_empty()));
    }

    #[tokio::test]
    async fn playlist_save_view_remove_delete() {
        use player::{NowPlaying, Player, QueueItem};
        let dir = tempfile::tempdir().unwrap();
        let store = Store::new(dir.path().to_path_buf());
        let (player, handle, snap) = Player::new().unwrap();
        drop(player);
        {
            let mut s = snap.lock().unwrap();
            s.now_playing = Some(NowPlaying {
                title: "Cur".into(),
                elapsed: Duration::ZERO,
                duration: None,
                request: "req-cur".into(),
            });
            s.upcoming = vec![QueueItem { title: "Up".into(), duration: None, request: "req-up".into() }];
        }
        assert!(handle_playlist(&toks("save mix"), &store, &handle, &snap).await.contains("已保存"));
        assert!(handle_playlist(&toks("list"), &store, &handle, &snap).await.contains("mix"));
        let v = handle_playlist(&toks("view mix"), &store, &handle, &snap).await;
        assert!(v.contains("1. Cur") && v.contains("2. Up"));
        assert!(handle_playlist(&toks("remove mix 1"), &store, &handle, &snap).await.contains("Cur"));
        assert!(handle_playlist(&toks("view mix"), &store, &handle, &snap).await.contains("1. Up"));
        assert!(handle_playlist(&toks("delete mix"), &store, &handle, &snap).await.contains("已删除"));
        assert!(handle_playlist(&toks("list"), &store, &handle, &snap).await.contains("还没有"));
        // 非法名
        assert!(handle_playlist(&toks("save a/b"), &store, &handle, &snap).await.contains("失败"));
    }
```

- [ ] **Step 5: bin 接线**

`crates/tsbot/Cargo.toml` 的 `[dependencies]` 增 `tsbot-playlist = { path = "../playlist" }`。

`crates/tsbot/src/main.rs`：在 `Player::new()` 之后、`tokio::spawn(commands::run(...))` 之前构造 Store，并把 spawn 调用加 store 参数。把：
```rust
    let (mut player, handle, snapshot) = player::Player::new()?;
    let (chat_tx, chat_rx) = mpsc::channel(32);
    let (reply_tx, reply_rx) = mpsc::channel(32);

    tokio::spawn(commands::run(chat_rx, handle, snapshot, reply_tx));
```
改为：
```rust
    let (mut player, handle, snapshot) = player::Player::new()?;
    let store = playlist::Store::new(std::path::PathBuf::from(config.playlist.dir));
    let (chat_tx, chat_rx) = mpsc::channel(32);
    let (reply_tx, reply_rx) = mpsc::channel(32);

    tokio::spawn(commands::run(chat_rx, handle, snapshot, store, reply_tx));
```

- [ ] **Step 6: 构建 + 全测试 + clippy**

Run: `cargo build --workspace 2>&1 | tail -6 && cargo test --workspace 2>&1 | grep -E "test result" | grep -oE "[0-9]+ passed" | awk '{s+=$1} END {print s" passed"}' && cargo clippy --workspace 2>&1 | grep -ciE "warning|error"`
Expected: 编译成功；全绿（commands 增 2 个测试函数）；clippy 0。

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(commands): !playlist 命令集 + bin 接线 Store"
```

---

## Task 5: 全量校验与人工验收

**Files:** 无代码改动

- [ ] **Step 1: 全量构建/测试/lint**

Run: `cargo build --workspace 2>&1 | tail -3 && cargo test --workspace 2>&1 | tail -3 && cargo clippy --workspace 2>&1 | grep -ciE "warning|error"`
Expected: 全绿；clippy 0。

- [ ] **Step 2: 人工验收（真实服务器）**

```bash
RUST_LOG=info cargo run -p tsbot -- --config config.toml
```
频道依次：
- `!play <url1>` `!play <url2>` 攒队列 → `!playlist save mylist` → 回"已保存歌单 mylist（N 首）"。
- 检查本地 `playlists/mylist.toml` 内容（request + title 可读）。
- `!playlist list` → 显示 mylist；`!playlist view mylist` → 编号 + 标题。
- `!stop`（清空）→ `!playlist play mylist` → 重解析后追加播放，回成功/失败数。
- `!playlist create p2` → `!playlist add p2 <url>`（回标题）→ `!playlist view p2` → `!playlist remove p2 1` → `!playlist delete p2`。
- 非法名 `!playlist save a/b` → 友好错误。

- [ ] **Step 3: 记录结果**

全流程通过即 2B 完成。失败按现象排查（保存的 request 为直连 URL→检查 Resolved.request 是否取原始 arg；view 标题缺失→检查 save 时快照 request/title；歌单文件格式→看 `[[items]]` 是否正常）。

---

## Self-Review 记录
- **Spec 覆盖**：Store/格式/校验(Task1)、Resolved+request 前提(Task2)、config dir(Task3)、!playlist 8 子命令+save-from-snapshot+bin(Task4)、人工验收(Task5)。逐条对应。
- **占位符扫描**：无 TODO/TBD；每步含完整代码与命令。
- **类型一致性**：`PlaylistItem{request,title}`、`Playlist{name,items}`、`Store::{new,save,load,list,delete}`、`valid_name`、`Resolved{input,title,duration,request}`、`NowPlaying/QueueItem` 加 `request`、`Command::Playlist(Vec<String>)`、`run(...,store,...)`、`handle_command(...,store)`、`handle_playlist`、`playlist_from_snapshot`、`config.playlist.dir` 跨任务一致。
- **绿色保持**：Task2 同步修 commands 测试字段；Task4 修 run 测试新增 store 参数——每步可编译可测。
- **风险**：toml `[[items]]` 往返（标准，Task1 测试覆盖）；歌单名安全校验（Task1 测试覆盖非法名）；大歌单 play 逐条解析耗时（spec 已记，人工验收）。
```
