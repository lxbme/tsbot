# Phase 1 Implementation Plan：薄端到端切片（可点播 + 常驻）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 频道内 `!play <url|path>` 入队、顺序播放、`!skip`/`!stop`/`!queue` 控制，机器人常驻并断线自动重连。

**Architecture:** 新增 source/player/commands 三子包；player 实现已有 `OpusSource`，由演进后的 `ts_connection::run_persistent` 驱动。三条单向 mpsc 通道（chat: driver→cmd、control: cmd→player、reply: cmd→driver）+ 只读 `Arc<Mutex<Snapshot>>` 解耦并发，无 `&mut con`/`&mut player` 争用。

**Tech Stack:** Rust workspace、tsclientlib 0.2（已 re-export `ts_bookkeeping::*`）、tokio mpsc/process、yt-dlp、ffmpeg、anyhow、tracing。

**前置:** Phase 0 已合并入 `main`（四子包，8 测试绿）。`/tmp/tscl_probe` 有 tsclientlib 源码可查 API。

---

## 已核验的 tsclientlib 事实（实现时据此，勿臆造）

- 收消息：匹配 `StreamItem::BookEvents(Vec<events::Event>)`，其中 `tsclientlib::events::Event::Message { target: MessageTarget, invoker: Invoker, message: String }`。`MessageTarget::Channel` 为频道消息；`Invoker { name, id: ClientId, uid }`。
- 自身 id：`con.get_state()?.own_client`（`ClientId`）。
- 发频道消息：`tsclientlib` 对 `ts_bookkeeping::*` 做了 `pub use`，故可用 `tsclientlib::messages::c2s::OutSendTextMessageMessage::new(...)` 构造 target=`tsclientlib::TextMessageTargetMode::Channel` 的命令，经 `tsclientlib::OutCommandExt::send(&mut con)` 发送。**实现时先验证** `c2s::OutSendTextMessageMessage::new` 与 `OutSendTextMessagePart{target,target_client_id,message}` 为 pub；若不可达，回退用 `con.get_state()?.clients.get(&invoker_id).unwrap().send_textmessage(text).send(&mut con)` 私聊发起者。
- `Identity` 实现 `Clone`。
- 上述类型通过 `tsclientlib` glob re-export 可达：`events`、`MessageTarget`、`Invoker`、`ClientId`、`messages::c2s`、`TextMessageTargetMode`、`OutCommandExt`。

---

## File Structure（Phase 1 后）

```
crates/source/Cargo.toml, src/lib.rs            Resolved + resolve()
crates/player/Cargo.toml, src/lib.rs            Control/PlayerHandle/Snapshot/Player(OpusSource)
crates/commands/Cargo.toml, src/lib.rs          Command/parse/format_queue/run
crates/audio/src/frame.rs                       spawn_ffmpeg 改返回 ChildStdout + kill_on_drop
crates/ts-connection/src/lib.rs                 +ChatMessage +run +run_persistent +send_channel_text；OpusSource None 语义
crates/config/src/lib.rs                        删除 Playback
crates/tsbot/src/main.rs, Cargo.toml            接线 player+commands+run_persistent
config.example.toml                             删除 [playback]
Cargo.toml                                       workspace members 增 3 子包
```

---

## Task 1: source 子包（解析音源）

**Files:** Create `crates/source/Cargo.toml`, `crates/source/src/lib.rs`；Modify 根 `Cargo.toml`

- [ ] **Step 1: 写 crates/source/Cargo.toml**

```toml
[package]
name = "tsbot-source"
version = "0.1.0"
edition = "2021"

[lib]
name = "source"

[dependencies]
tokio = { workspace = true, features = ["process", "macros", "rt"] }
anyhow = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
```

- [ ] **Step 2: 写实现 + 测试 crates/source/src/lib.rs**

```rust
use std::path::Path;
use std::process::Stdio;

use anyhow::{bail, Context, Result};
use tokio::process::Command;

/// 一个可交给 ffmpeg 播放的条目。
pub struct Resolved {
    /// ffmpeg `-i` 的参数：本地路径 / 直连或解析后的媒体 URL。
    pub input: String,
    /// 队列展示用；Phase 1 = 原始请求串。
    pub label: String,
}

/// 解析用户参数为可播条目。
/// - 本地存在文件 → 直接用其路径
/// - http(s) → `yt-dlp -g` 取直连 URL；失败则回退用原始 URL（直连流/电台）
/// - 其它 → 错误
pub async fn resolve(arg: &str) -> Result<Resolved> {
    if Path::new(arg).is_file() {
        return Ok(Resolved { input: arg.to_string(), label: arg.to_string() });
    }
    if arg.starts_with("http://") || arg.starts_with("https://") {
        let input = match ytdlp_direct_url(arg).await {
            Ok(url) => url,
            Err(_) => arg.to_string(), // 回退：当作直连流
        };
        return Ok(Resolved { input, label: arg.to_string() });
    }
    bail!("无法识别的音源: {arg}（需为存在的本地文件或 http(s) URL）")
}

async fn ytdlp_direct_url(url: &str) -> Result<String> {
    let out = Command::new("yt-dlp")
        .args(["-g", "-f", "bestaudio/best", url])
        .stdin(Stdio::null())
        .output()
        .await
        .context("启动 yt-dlp 失败")?;
    if !out.status.success() {
        bail!("yt-dlp 解析失败: {}", String::from_utf8_lossy(&out.stderr));
    }
    String::from_utf8_lossy(&out.stdout)
        .lines()
        .next()
        .map(|s| s.trim().to_string())
        .filter(|s| !s.is_empty())
        .context("yt-dlp 未返回 URL")
}

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn resolves_existing_local_file() {
        let dir = tempfile::tempdir().unwrap();
        let path = dir.path().join("song.mp3");
        std::fs::write(&path, b"x").unwrap();
        let p = path.to_str().unwrap();
        let r = resolve(p).await.unwrap();
        assert_eq!(r.input, p);
        assert_eq!(r.label, p);
    }

    #[tokio::test]
    async fn rejects_non_file_non_url() {
        let err = resolve("not a url or file").await;
        assert!(err.is_err());
    }
}
```

- [ ] **Step 3: 加入 workspace members**

根 `Cargo.toml` 的 members 改为（在末尾追加 source）：
```toml
members = ["crates/config", "crates/audio", "crates/ts-connection", "crates/source", "crates/tsbot"]
```

- [ ] **Step 4: 测试**

Run: `cargo test -p tsbot-source 2>&1 | tail -8`
Expected: 2 个测试 PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(source): 音源解析 resolve（本地/yt-dlp/直连流）"
```

---

## Task 2: audio — spawn_ffmpeg 返回具体类型 + kill_on_drop

**Files:** Modify `crates/audio/src/frame.rs`

- [ ] **Step 1: 修改 spawn_ffmpeg 签名与实现**

把 `crates/audio/src/frame.rs` 中的 `spawn_ffmpeg` 函数整体替换为（返回具体 `ChildStdout`，并 `kill_on_drop(true)` 使丢弃子进程即终止 ffmpeg）：

```rust
/// 启动 ffmpeg 把输入解码为 48kHz/单声道/f32le 裸 PCM，返回子进程与其 stdout。
/// 输入可为本地路径或 URL（ffmpeg `-i` 通用）。子进程在 drop 时被杀。
pub fn spawn_ffmpeg(input: &str) -> Result<(Child, tokio::process::ChildStdout)> {
    let mut child = Command::new("ffmpeg")
        .args([
            "-hide_banner", "-loglevel", "error",
            "-i", input,
            "-ac", "1", "-ar", "48000", "-f", "f32le", "-",
        ])
        .stdin(Stdio::null())
        .stdout(Stdio::piped())
        .kill_on_drop(true)
        .spawn()?;
    let stdout = child.stdout.take().expect("stdout piped");
    Ok((child, stdout))
}
```

> 注：`crates/tsbot/src/main.rs` 当前的 `FileOpusSource` 用 `PcmFrameReader::new(stdout)`，`ChildStdout` 实现 `AsyncRead`，仍可编译（该文件将在 Task 6 重写）。

- [ ] **Step 2: 构建 + audio 测试**

Run: `cargo build -p tsbot-audio 2>&1 | tail -3 && cargo test -p tsbot-audio 2>&1 | tail -6`
Expected: 编译成功；frame/encode 4 测试仍 PASS（它们不调用 spawn_ffmpeg）。

- [ ] **Step 3: 全 workspace 编译确认 bin 未被破坏**

Run: `cargo build --workspace 2>&1 | tail -4`
Expected: 成功（FileOpusSource 仍兼容 ChildStdout）。

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "refactor(audio): spawn_ffmpeg 返回具体 ChildStdout 并 kill_on_drop"
```

---

## Task 3: player 子包（队列 + 播放引擎）

**Files:** Create `crates/player/Cargo.toml`, `crates/player/src/lib.rs`；Modify 根 `Cargo.toml`

- [ ] **Step 1: 写 crates/player/Cargo.toml**

```toml
[package]
name = "tsbot-player"
version = "0.1.0"
edition = "2021"

[lib]
name = "player"

[dependencies]
tsbot-source = { path = "../source" }
tsbot-audio = { path = "../audio" }
tsbot-ts-connection = { path = "../ts-connection" }
tokio = { workspace = true, features = ["sync", "process", "macros", "rt"] }
anyhow = { workspace = true }
tracing = { workspace = true }
```

- [ ] **Step 2: 写实现 + 测试 crates/player/src/lib.rs**

```rust
use std::collections::VecDeque;
use std::sync::{Arc, Mutex};

use anyhow::Result;
use tokio::process::{Child, ChildStdout};
use tokio::sync::mpsc;

use source::Resolved;
use tsbot_audio::{spawn_ffmpeg, OpusMusicEncoder, PcmFrameReader};
use ts_connection::OpusSource;

/// 控制命令，经 control 通道从指令处理器发往 player。
pub enum Control {
    Play(Resolved),
    Skip,
    Stop,
}

/// 指令处理器持有的句柄（克隆其内部 control_tx）。
#[derive(Clone)]
pub struct PlayerHandle {
    tx: mpsc::Sender<Control>,
}

impl PlayerHandle {
    pub fn play(&self, r: Resolved) {
        let _ = self.tx.try_send(Control::Play(r));
    }
    pub fn skip(&self) {
        let _ = self.tx.try_send(Control::Skip);
    }
    pub fn stop(&self) {
        let _ = self.tx.try_send(Control::Stop);
    }
}

/// 供 `!queue` 只读的播放快照。
#[derive(Clone, Default)]
pub struct Snapshot {
    pub now_playing: Option<String>,
    pub upcoming: Vec<String>,
}

/// 正在播放的曲目：ffmpeg 子进程 + 帧读取器 + 展示名。
struct Current {
    _child: Child, // 持有以保活；drop 时 kill_on_drop 终止 ffmpeg
    reader: PcmFrameReader<ChildStdout>,
    label: String,
}

/// 队列播放引擎；driver 持 `&mut Player` 调 next_frame。
pub struct Player {
    rx: mpsc::Receiver<Control>,
    queue: VecDeque<Resolved>,
    current: Option<Current>,
    encoder: OpusMusicEncoder,
    snapshot: Arc<Mutex<Snapshot>>,
}

impl Player {
    /// 返回 player、配套句柄、只读快照。
    pub fn new() -> Result<(Player, PlayerHandle, Arc<Mutex<Snapshot>>)> {
        let (tx, rx) = mpsc::channel(32);
        let snapshot = Arc::new(Mutex::new(Snapshot::default()));
        let player = Player {
            rx,
            queue: VecDeque::new(),
            current: None,
            encoder: OpusMusicEncoder::new()?,
            snapshot: snapshot.clone(),
        };
        Ok((player, PlayerHandle { tx }, snapshot))
    }

    /// 排空 control 通道，更新队列与快照。同步、不 await（丢弃 Current 即杀 ffmpeg）。
    fn drain_control(&mut self) {
        while let Ok(c) = self.rx.try_recv() {
            match c {
                Control::Play(r) => self.queue.push_back(r),
                Control::Skip => self.current = None,
                Control::Stop => {
                    self.queue.clear();
                    self.current = None;
                }
            }
        }
        self.update_snapshot();
    }

    fn update_snapshot(&self) {
        let mut s = self.snapshot.lock().unwrap();
        s.now_playing = self.current.as_ref().map(|c| c.label.clone());
        s.upcoming = self.queue.iter().map(|r| r.label.clone()).collect();
    }
}

impl OpusSource for Player {
    async fn next_frame(&mut self) -> Result<Option<Vec<u8>>> {
        self.drain_control();
        loop {
            // 无当前曲目则尝试起播下一条
            if self.current.is_none() {
                match self.queue.pop_front() {
                    Some(r) => match spawn_ffmpeg(&r.input) {
                        Ok((child, stdout)) => {
                            self.current = Some(Current {
                                _child: child,
                                reader: PcmFrameReader::new(stdout),
                                label: r.label,
                            });
                            self.update_snapshot();
                        }
                        Err(e) => {
                            tracing::warn!(%e, "spawn ffmpeg 失败，跳过该曲");
                            self.update_snapshot();
                            continue;
                        }
                    },
                    None => return Ok(None), // 空闲：本 tick 无帧
                }
            }
            // 有当前曲目：取一帧
            let frame_opt = self.current.as_mut().unwrap().reader.next_frame().await?;
            match frame_opt {
                Some(frame) => return Ok(Some(self.encoder.encode(&frame)?.to_vec())),
                None => {
                    // 当前曲目结束，前进
                    self.current = None;
                    self.update_snapshot();
                    continue;
                }
            }
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn res(label: &str) -> Resolved {
        Resolved { input: format!("/nonexistent/{label}"), label: label.to_string() }
    }

    #[tokio::test]
    async fn idle_player_yields_none() {
        let (mut player, _handle, _snap) = Player::new().unwrap();
        assert!(player.next_frame().await.unwrap().is_none());
    }

    #[tokio::test]
    async fn play_enqueues_and_updates_snapshot() {
        let (mut player, handle, snap) = Player::new().unwrap();
        handle.play(res("A"));
        handle.play(res("B"));
        player.drain_control();
        let s = snap.lock().unwrap();
        assert_eq!(s.upcoming, vec!["A".to_string(), "B".to_string()]);
        assert!(s.now_playing.is_none());
    }

    #[tokio::test]
    async fn stop_clears_queue() {
        let (mut player, handle, snap) = Player::new().unwrap();
        handle.play(res("A"));
        player.drain_control();
        handle.stop();
        player.drain_control();
        assert!(snap.lock().unwrap().upcoming.is_empty());
    }
}
```

- [ ] **Step 3: 加入 members**

根 `Cargo.toml` members 追加 `crates/player`（置于 source 之后、tsbot 之前）：
```toml
members = ["crates/config", "crates/audio", "crates/ts-connection", "crates/source", "crates/player", "crates/tsbot"]
```

- [ ] **Step 4: 测试**

Run: `cargo test -p tsbot-player 2>&1 | tail -8`
Expected: 3 测试 PASS（idle→None、入队更新快照、stop 清空）。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(player): 队列播放引擎，实现 OpusSource"
```

---

## Task 4: commands 子包（解析 + 处理器）

**Files:** Create `crates/commands/Cargo.toml`, `crates/commands/src/lib.rs`；Modify 根 `Cargo.toml`

> 依赖 ts-connection 的 `ChatMessage`，该类型在 Task 5 加入。**本任务先用一个本地最小定义编译/测试 parse 与 format_queue，run 函数留到 Task 5 完成后接 `ChatMessage`**——为避免双向依赖与返工，本任务只实现并测试 `parse` 与 `format_queue`（纯函数），`run` 的实现与接线在 Task 5 末尾补齐（届时 ChatMessage 已存在）。

- [ ] **Step 1: 写 crates/commands/Cargo.toml**

```toml
[package]
name = "tsbot-commands"
version = "0.1.0"
edition = "2021"

[lib]
name = "commands"

[dependencies]
tsbot-player = { path = "../player" }
tsbot-source = { path = "../source" }
tsbot-ts-connection = { path = "../ts-connection" }
tokio = { workspace = true, features = ["sync", "macros", "rt"] }
```

- [ ] **Step 2: 写 parse + format_queue + 测试 crates/commands/src/lib.rs**

```rust
use player::Snapshot;

/// 解析后的指令。
pub enum Command {
    Play(String),
    Skip,
    Stop,
    Queue,
}

/// 解析一行文本；非指令（不以 ! 开头或未知/缺参）返回 None。
pub fn parse(text: &str) -> Option<Command> {
    let rest = text.trim().strip_prefix('!')?;
    let mut it = rest.splitn(2, char::is_whitespace);
    let cmd = it.next()?.to_ascii_lowercase();
    let arg = it.next().map(str::trim).unwrap_or("");
    match cmd.as_str() {
        "play" if !arg.is_empty() => Some(Command::Play(arg.to_string())),
        "skip" => Some(Command::Skip),
        "stop" => Some(Command::Stop),
        "queue" => Some(Command::Queue),
        _ => None,
    }
}

/// 把快照格式化为 `!queue` 的回复文本。
pub fn format_queue(s: &Snapshot) -> String {
    let mut out = match &s.now_playing {
        Some(np) => format!("正在播放: {np}"),
        None => "当前没有播放".to_string(),
    };
    if s.upcoming.is_empty() {
        out.push_str("\n队列为空");
    } else {
        out.push_str("\n队列:");
        for (i, label) in s.upcoming.iter().enumerate() {
            out.push_str(&format!("\n  {}. {}", i + 1, label));
        }
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parse_play_with_arg() {
        assert!(matches!(parse("!play http://x"), Some(Command::Play(a)) if a == "http://x"));
    }

    #[test]
    fn parse_play_multiword_arg_trimmed() {
        assert!(matches!(parse("  !play  a b  "), Some(Command::Play(a)) if a == "a b"));
    }

    #[test]
    fn parse_play_without_arg_is_none() {
        assert!(parse("!play").is_none());
    }

    #[test]
    fn parse_skip_stop_queue_case_insensitive() {
        assert!(matches!(parse("!skip"), Some(Command::Skip)));
        assert!(matches!(parse("!STOP"), Some(Command::Stop)));
        assert!(matches!(parse("!Queue"), Some(Command::Queue)));
    }

    #[test]
    fn parse_non_command_is_none() {
        assert!(parse("hello world").is_none());
        assert!(parse("!unknown").is_none());
    }

    #[test]
    fn format_queue_empty_and_filled() {
        let empty = Snapshot::default();
        assert!(format_queue(&empty).contains("没有播放"));
        let filled = Snapshot { now_playing: Some("A".into()), upcoming: vec!["B".into()] };
        let s = format_queue(&filled);
        assert!(s.contains("正在播放: A"));
        assert!(s.contains("1. B"));
    }
}
```

- [ ] **Step 3: 加入 members**

根 `Cargo.toml` members 追加 `crates/commands`（置于 player 之后、tsbot 之前）：
```toml
members = ["crates/config", "crates/audio", "crates/ts-connection", "crates/source", "crates/player", "crates/commands", "crates/tsbot"]
```

- [ ] **Step 4: 测试**

Run: `cargo test -p tsbot-commands 2>&1 | tail -10`
Expected: parse/format_queue 共 6 测试 PASS。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(commands): 指令解析 parse 与 format_queue"
```

---

## Task 5: ts-connection — 收发文本、run、run_persistent，并补全 commands::run

**Files:** Modify `crates/ts-connection/src/lib.rs`, `crates/ts-connection/Cargo.toml`；Modify `crates/commands/src/lib.rs`

- [ ] **Step 1: 验证频道发消息构造可用**

Run: `grep -rn "OutSendTextMessageMessage\|OutSendTextMessagePart" /tmp/tscl_probe/utils/ts-bookkeeping/src 2>/dev/null | head`
Expected: 看到 `OutSendTextMessageMessage::new` 与 `OutSendTextMessagePart { target, target_client_id, message }`。若其字段/构造不是 pub（编译报错），改用回退：`con.get_state()?.clients.get(&invoker_id)` 取 `Client` 调 `.send_textmessage(text).send(con)` 私聊发起者（并在 Step 2 的 `send_channel_text` 内改为私聊回退实现）。

- [ ] **Step 2: 在 crates/ts-connection/src/lib.rs 增加收发与驱动**

在 `crates/ts-connection/src/lib.rs` 顶部 `use` 区补充：

```rust
use std::future::Future;

use tokio::sync::mpsc;
use tsclientlib::events::Event;
use tsclientlib::messages::c2s;
use tsclientlib::{ClientId, MessageTarget, OutCommandExt, TextMessageTargetMode};
```

并把现有的 `stream_audio` 函数**整体删除**，替换为以下内容（`OpusSource`、`opus_music_packet`、`connect`、`wait_until_ready`、`ConnectSettings` 保留，但给 `ConnectSettings` 加 `#[derive(Clone)]`）：

```rust
/// 收到的频道文本消息，driver 转发给指令处理器。
pub struct ChatMessage {
    pub text: String,
    pub invoker_id: ClientId,
}

/// 向机器人所在频道发送一条文本。
fn send_channel_text(con: &mut Connection, text: &str) -> Result<()> {
    let cmd = c2s::OutSendTextMessageMessage::new(&mut std::iter::once(c2s::OutSendTextMessagePart {
        target: TextMessageTargetMode::Channel,
        target_client_id: None,
        message: text.into(),
    }));
    cmd.send(con)?;
    Ok(())
}

/// 驱动单条连接直到断线返回 Err：
/// - 20ms 拉帧发送（source.next_frame；None=本 tick 空闲，循环继续）
/// - 事件流中的频道文本消息（非自身）转发到 chat_tx
/// - drain reply_rx，把回复发回所在频道
pub async fn run<S: OpusSource>(
    con: &mut Connection,
    source: &mut S,
    chat_tx: &mpsc::Sender<ChatMessage>,
    reply_rx: &mut mpsc::Receiver<String>,
) -> Result<()> {
    let own_id = con.get_state()?.own_client;
    let mut interval = tokio::time::interval(Duration::from_millis(20));
    loop {
        // 1. 发送待回复（需 &mut con，先于事件借用）
        while let Ok(text) = reply_rx.try_recv() {
            if let Err(e) = send_channel_text(con, &text) {
                tracing::warn!(%e, "发送回复失败");
            }
        }

        // 2. select：节奏 tick 或事件流（事件流内转发频道消息）
        let events = con.events().try_for_each(|item| {
            if let StreamItem::BookEvents(evs) = &item {
                for e in evs {
                    if let Event::Message { target: MessageTarget::Channel, invoker, message } = e {
                        if invoker.id != own_id {
                            let _ = chat_tx.try_send(ChatMessage {
                                text: message.clone(),
                                invoker_id: invoker.id,
                            });
                        }
                    }
                }
            }
            future::ready(Ok(()))
        });
        tokio::select! {
            _ = interval.tick() => {}
            r = events => { r?; bail!("Disconnected"); }
        }

        // 3. 拉帧发送（events 已 drop，借用释放）；None 表示空闲，不发
        if let Some(data) = source.next_frame().await? {
            con.send_audio(opus_music_packet(&data))?;
        }
    }
}

/// 常驻：connect → wait_until_ready → run；断线后指数退避重连，直到 shutdown。
/// source 与 channels 跨重连复用。
pub async fn run_persistent<S: OpusSource>(
    settings: ConnectSettings,
    source: &mut S,
    chat_tx: mpsc::Sender<ChatMessage>,
    mut reply_rx: mpsc::Receiver<String>,
    shutdown: impl Future<Output = ()>,
) -> Result<()> {
    tokio::pin!(shutdown);
    let mut backoff = Duration::from_secs(1);
    loop {
        let attempt = async {
            let mut con = connect(settings.clone())?;
            wait_until_ready(&mut con).await?;
            tracing::info!("connected");
            backoff_reset(&mut backoff);
            run(&mut con, source, &chat_tx, &mut reply_rx).await
        };
        tokio::select! {
            _ = &mut shutdown => { tracing::info!("shutdown"); return Ok(()); }
            res = attempt => {
                if let Err(e) = res {
                    tracing::warn!(%e, ?backoff, "连接断开，准备重连");
                }
            }
        }
        tokio::select! {
            _ = &mut shutdown => return Ok(()),
            _ = tokio::time::sleep(backoff) => {}
        }
        backoff = (backoff * 2).min(Duration::from_secs(30));
    }
}

fn backoff_reset(backoff: &mut Duration) {
    *backoff = Duration::from_secs(1);
}
```

并把 `ConnectSettings` 定义加上 `Clone`：

```rust
#[derive(Clone)]
pub struct ConnectSettings {
    pub address: String,
    pub password: Option<String>,
    pub channel: Option<String>,
    pub name: String,
    pub identity: Identity,
}
```

同时把 `OpusSource::next_frame` 的文档注释更新为：`None` 表示"本 tick 无帧可发（空闲），driver 应继续循环而非结束"。

- [ ] **Step 3: 更新 crates/ts-connection/Cargo.toml 依赖 feature**

确保 tokio 含 `sync`（mpsc）。把其 `tokio` 行改为：
```toml
tokio = { workspace = true, features = ["time", "macros", "sync"] }
```

- [ ] **Step 4: 补全 commands::run（现在 ChatMessage 已存在）**

在 `crates/commands/src/lib.rs` 顶部 `use` 补充：
```rust
use std::sync::{Arc, Mutex};

use player::{PlayerHandle, Snapshot};
use tokio::sync::mpsc;
use ts_connection::ChatMessage;
```

并在 `format_queue` 之后、`#[cfg(test)]` 之前插入处理循环：

```rust
/// 指令处理循环：读 chat_rx → parse → 执行 → 经 reply_tx 回复。不接触 con。
pub async fn run(
    mut chat_rx: mpsc::Receiver<ChatMessage>,
    handle: PlayerHandle,
    snapshot: Arc<Mutex<Snapshot>>,
    reply_tx: mpsc::Sender<String>,
) {
    while let Some(msg) = chat_rx.recv().await {
        let Some(cmd) = parse(&msg.text) else { continue };
        let reply = match cmd {
            Command::Play(arg) => match source::resolve(&arg).await {
                Ok(r) => {
                    let label = r.label.clone();
                    handle.play(r);
                    format!("已添加: {label}")
                }
                Err(e) => format!("解析失败: {e}"),
            },
            Command::Skip => {
                handle.skip();
                "已跳过".to_string()
            }
            Command::Stop => {
                handle.stop();
                "已停止并清空队列".to_string()
            }
            Command::Queue => {
                let snap = snapshot.lock().unwrap().clone();
                format_queue(&snap)
            }
        };
        let _ = reply_tx.send(reply).await;
    }
}
```

在 commands 的测试模块末尾追加一个 run 的冒烟测试（不依赖网络，验证 `!skip`/`!queue` 的回复与对 player 的作用）：

```rust
    #[tokio::test]
    async fn run_dispatches_skip_and_queue() {
        use player::Player;

        let (player, handle, snap) = Player::new().unwrap();
        drop(player); // 本测试只验证句柄与回复，不驱动 player
        let (chat_tx, chat_rx) = mpsc::channel(8);
        let (reply_tx, mut reply_rx) = mpsc::channel(8);
        let join = tokio::spawn(run(chat_rx, handle, snap, reply_tx));

        chat_tx.send(ChatMessage { text: "!queue".into(), invoker_id: ts_connection::ClientId(1) }).await.unwrap();
        let r = reply_rx.recv().await.unwrap();
        assert!(r.contains("没有播放"));

        chat_tx.send(ChatMessage { text: "!skip".into(), invoker_id: ts_connection::ClientId(1) }).await.unwrap();
        assert_eq!(reply_rx.recv().await.unwrap(), "已跳过");

        drop(chat_tx);
        let _ = join.await;
    }
```

> 注：测试用 `ts_connection::ClientId(1)` 构造 `ChatMessage.invoker_id`。`ClientId` 是元组结构体（`ClientId(u16)`），由 tsclientlib re-export。实现时若其构造子非 pub，改用 `Default` 或从 player 侧不校验 id 的方式；此字段在 run 中未被使用，仅为构造消息。

- [ ] **Step 5: 构建 + 测试**

Run: `cargo build --workspace 2>&1 | tail -6 && cargo test -p tsbot-ts-connection -p tsbot-commands 2>&1 | tail -12`
Expected: 编译成功（注意 bin 仍引用已删除的 `stream_audio`/`FileOpusSource` → **bin 会编译失败**，这是预期：Task 6 重写 bin）。若要本步绿，仅编译/测试这两个包：`cargo test -p tsbot-ts-connection` 与 `cargo test -p tsbot-commands`（commands 含新增 run 冒烟测试 + 之前 6 个）。

> 因为删除 `stream_audio` 会使 bin 暂时编译失败，本任务的提交是一个"中间态"。这是可接受的：Task 6 紧接着修复 bin。若偏好每步全绿，可把 Task 5 与 Task 6 合并为一次提交。

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(ts-connection): ChatMessage + run + run_persistent + 频道收发；补全 commands::run"
```

---

## Task 6: bin 接线 + 删除 [playback]

**Files:** Modify `crates/tsbot/src/main.rs`, `crates/tsbot/Cargo.toml`, `crates/config/src/lib.rs`, `config.example.toml`

- [ ] **Step 1: 删除 config 的 Playback**

把 `crates/config/src/lib.rs` 中 `Config` 的 `playback` 字段与 `Playback` 结构体删除，并删除测试里对 `playback`/`file` 的断言与 TOML 中的 `[playback]` 段。最终 `Config`：

```rust
#[derive(Debug, Deserialize)]
pub struct Config {
    pub server: Server,
    pub bot: Bot,
}
```

并把两个测试的 TOML 字面量删掉 `[playback]` 段、断言里删掉 `c.playback...` 那行（`loads_full_config` 保留 server/bot 断言；`loads_config_without_optionals` 不变其 optional 断言）。

- [ ] **Step 2: 更新 crates/tsbot/Cargo.toml 依赖**

把 `[dependencies]` 替换为：

```toml
[dependencies]
tsbot-config = { path = "../config" }
tsbot-ts-connection = { path = "../ts-connection" }
tsbot-player = { path = "../player" }
tsbot-commands = { path = "../commands" }
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "signal", "sync"] }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
clap = { workspace = true }
```

- [ ] **Step 3: 重写 crates/tsbot/src/main.rs**

```rust
use std::path::{Path, PathBuf};

use anyhow::Result;
use clap::Parser;
use tokio::sync::mpsc;
use ts_connection::ConnectSettings;

/// 命令行参数：仅配置文件路径。
#[derive(Parser, Debug)]
#[command(about = "TS3 musicbot")]
struct Args {
    /// TOML 配置文件路径
    #[arg(short, long, default_value = "config.toml")]
    config: PathBuf,
}

#[tokio::main]
async fn main() -> Result<()> {
    tracing_subscriber::fmt::init();
    let args = Args::parse();
    let config = tsbot_config::load(&args.config)?;

    let identity = ts_connection::identity::load_or_create(Path::new(&config.bot.identity_path))?;
    let settings = ConnectSettings {
        address: config.server.address,
        password: config.server.password,
        channel: config.server.channel,
        name: config.bot.name,
        identity,
    };

    // player（OpusSource）+ 三条通道
    let (mut player, handle, snapshot) = player::Player::new()?;
    let (chat_tx, chat_rx) = mpsc::channel(32);
    let (reply_tx, reply_rx) = mpsc::channel(32);

    // 指令处理器：独立任务（不接触 con）
    tokio::spawn(commands::run(chat_rx, handle, snapshot, reply_tx));

    // 常驻驱动，ctrl_c 关闭
    let shutdown = async {
        let _ = tokio::signal::ctrl_c().await;
    };
    ts_connection::run_persistent(settings, &mut player, chat_tx, reply_rx, shutdown).await
}
```

- [ ] **Step 4: 更新 config.example.toml（删除 [playback]）**

```toml
[server]
address  = "ts.example.com:9987"
password = "secret"     # 可选；无密码服务器删除此行
channel  = "Lobby"      # 可选；不指定则留在默认频道

[bot]
name          = "tsbot"
identity_path = "identity.toml"
```

- [ ] **Step 5: 构建 + 全测试**

Run: `cargo build --workspace 2>&1 | tail -5 && cargo test --workspace 2>&1 | grep -E "test result" | grep -oE "[0-9]+ passed" | awk '{s+=$1} END {print s" passed"}'`
Expected: 编译成功；测试全绿。预期总数：source 2、audio 4、config 2、ts-connection 1、player 3、commands 7（6 + run 冒烟）= 19。

- [ ] **Step 6: clippy**

Run: `cargo clippy --workspace 2>&1 | grep -iE "warning|error" | head; echo done`
Expected: 无 warning/error。

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(tsbot): 接线 player+commands+run_persistent，删除 [playback]"
```

---

## Task 7: 全量校验与人工验收

**Files:** 无代码改动

- [ ] **Step 1: 全量构建/测试/lint + 依赖无环**

Run: `cargo build --workspace 2>&1 | tail -3 && cargo test --workspace 2>&1 | tail -3 && cargo tree -p tsbot --depth 1 2>&1 | head -20`
Expected: 全绿；tsbot 直接依赖 config/ts-connection/player/commands；库间方向无环（source 不依赖 player/commands；player 不依赖 commands）。

- [ ] **Step 2: 准备配置与音频**

```bash
cp config.example.toml config.toml
# 编辑 config.toml 填入真实服务器；准备一个本地 test.mp3
```

- [ ] **Step 3: 启动机器人（常驻）**

Run: `RUST_LOG=info cargo run -p tsbot -- --config config.toml`
Expected: 日志 `connected`；进程常驻不退出。

- [ ] **Step 4: 频道指令验收**

在 TS3 客户端进入机器人所在频道，依次发：
- `!play <test.mp3 的本地路径>` → 期望回复"已添加: …"，并**听到声音**。
- `!play <一个 YouTube 或直连流 URL>` → 期望入队，前一首播完后续播。
- `!queue` → 回复当前+待播列表。
- `!skip` → 切下一首。
- `!stop` → 停止并清空，机器人**仍在频道**。

- [ ] **Step 5: 重连验收**

临时断开网络（或重启 TS3 服务器）几秒后恢复。
Expected: 日志显示断开 + 退避重连，恢复后机器人重新出现在频道、可继续接受指令。

- [ ] **Step 6: 记录结果**

三项（点播出声 / 指令生效 / 自动重连）满足即 Phase 1 通过。失败按现象排查：
- 听不到声音但在线 → 检查发言权限、ffmpeg 是否能解码该输入（手动 `ffmpeg -i <input> -f f32le -` 验证）。
- 收不到指令 → 确认订阅了频道、`Event::Message` 是否到达（加日志）、是否被自身过滤误伤。
- 回复发不出 → 核对 `send_channel_text` 构造（Task 5 Step 1 的回退）。

---

## Self-Review 记录

- **Spec 覆盖**：source.resolve(Task1)、spawn_ffmpeg 泛化(Task2)、player+OpusSource+None 空闲语义(Task3)、commands.parse/run(Task4+5)、ts-connection ChatMessage/run/run_persistent/频道收发(Task5)、bin 接线+去 [playback](Task6)、断线重连(Task5 run_persistent)、人工验收(Task7)——逐条对应。
- **占位符扫描**：无 TODO/TBD；每个代码步骤含完整代码与确切命令。Task5 Step1 的"待验证"是带回退方案的核对步骤，非占位。
- **类型一致性**：`Resolved{input,label}`、`Control::{Play,Skip,Stop}`、`PlayerHandle::{play,skip,stop}`、`Snapshot{now_playing,upcoming}`、`Player::new()->Result<(...)>`、`OpusSource::next_frame()->Result<Option<Vec<u8>>>`、`ChatMessage{text,invoker_id}`、`run(con,source,&chat_tx,&mut reply_rx)`、`run_persistent(settings,source,chat_tx,reply_rx,shutdown)`、`commands::run(chat_rx,handle,snapshot,reply_tx)`、`ConnectSettings: Clone` 在各任务间一致。
- **中间态**：Task5 删 `stream_audio` 致 bin 暂不编译，Task6 修复——已在 Task5 Step5/6 标注，并提示可合并两任务。
- **风险**：频道发消息构造(Task5 Step1 带私聊回退)、`ClientId` 构造子可见性(Task5 Step4 注)、async-fn-in-trait 已在 Phase 0 用 `#[allow]`。
```
