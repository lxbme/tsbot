# Phase 3 Implementation Plan：权限系统

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 角色 + 单指令权限的访问控制（按 TS3 uid，opt-in：无配置段则全开），含 `!whoami` 引导。

**Architecture:** 新增 `crates/perms`（`Permissions` + `allows`/`role_of`，serde）。`ChatMessage` 带 `invoker_uid`，`commands::run` 在分发前用 perms 门禁，`help`/`whoami` 永远放行。config 增 `[permissions]`。

**Tech Stack:** 现有 workspace + serde（perms 配置）。

**前置:** Phase 2B 已合并 + 两个修复（电台 URL、全角冒号）。`main` 29 测试。

---

## 已核验
- `Invoker.uid: Option<UidBuf>`（tsproto-types）；`Uid: Display` 输出 base64 标准 uid 串（TS 用户可见的 uid）。`UidBuf` Deref→Uid，故 `uidbuf.to_string()` 经 autoderef 得到 base64 串。None → 空串。
- 触点：commands `parse`/`run`(行~145)/`handle_command`/测试里 `ChatMessage{text,invoker_id}` 构造；ts-connection `ChatMessage`(行~60) + driver 事件闭包；config `Config`；bin `commands::run` 调用。

## File Structure（Phase 3 后）
```
crates/perms/Cargo.toml, src/lib.rs   Permissions + allows/role_of + tests
crates/ts-connection/src/lib.rs       ChatMessage + invoker_uid；driver 填充
crates/config/src/lib.rs, Cargo.toml  Config + [permissions]；依赖 perms
crates/commands/src/lib.rs, Cargo.toml Command::Whoami + command_name + run 门禁；依赖 perms
crates/tsbot/src/main.rs, Cargo.toml  传 config.permissions
config.example.toml                   [permissions] 示例
```

---

## Task 1: crates/perms 子包

**Files:** Create `crates/perms/Cargo.toml`, `crates/perms/src/lib.rs`; Modify root `Cargo.toml`

- [ ] **Step 1: crates/perms/Cargo.toml**
```toml
[package]
name = "tsbot-perms"
version = "0.1.0"
edition = "2021"

[lib]
name = "perms"

[dependencies]
serde = { workspace = true }
```

- [ ] **Step 2: crates/perms/src/lib.rs（VERBATIM）**
```rust
use std::collections::HashMap;

use serde::Deserialize;

/// 角色 + 单指令权限。按 uid 判定，与 TS3 服务器组解耦。
#[derive(Debug, Clone, Deserialize, Default)]
pub struct Permissions {
    #[serde(default = "default_role")]
    pub default_role: String,
    /// 角色名 → 允许的指令名列表（含 "*" 表示全部）。
    #[serde(default)]
    pub roles: HashMap<String, Vec<String>>,
    /// uid → 角色名。
    #[serde(default)]
    pub users: HashMap<String, String>,
}

fn default_role() -> String {
    "guest".to_string()
}

impl Permissions {
    /// 是否允许某 uid 执行某指令。
    /// roles 为空（未配置权限）→ 一律放行（opt-in）。
    pub fn allows(&self, uid: &str, command: &str) -> bool {
        if self.roles.is_empty() {
            return true;
        }
        let role = self.role_of(uid);
        match self.roles.get(role) {
            Some(cmds) => cmds.iter().any(|c| c == "*" || c == command),
            None => false,
        }
    }

    /// uid 当前归属的角色名（未映射则 default_role）。
    pub fn role_of(&self, uid: &str) -> &str {
        self.users.get(uid).map(String::as_str).unwrap_or(&self.default_role)
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    fn perms() -> Permissions {
        let mut roles = HashMap::new();
        roles.insert("admin".to_string(), vec!["*".to_string()]);
        roles.insert("guest".to_string(), vec!["play".to_string(), "queue".to_string()]);
        let mut users = HashMap::new();
        users.insert("admin-uid".to_string(), "admin".to_string());
        Permissions { default_role: "guest".to_string(), roles, users }
    }

    #[test]
    fn disabled_when_no_roles() {
        let p = Permissions::default();
        assert!(p.allows("anyone", "stop"));
    }

    #[test]
    fn admin_wildcard_allows_all() {
        let p = perms();
        assert!(p.allows("admin-uid", "stop"));
        assert!(p.allows("admin-uid", "playlist"));
    }

    #[test]
    fn guest_limited() {
        let p = perms();
        assert!(p.allows("stranger", "play"));
        assert!(p.allows("stranger", "queue"));
        assert!(!p.allows("stranger", "stop"));
    }

    #[test]
    fn unknown_role_denies() {
        let mut p = perms();
        p.default_role = "ghost".to_string(); // 未定义角色
        assert!(!p.allows("stranger", "play"));
    }

    #[test]
    fn role_of_maps() {
        let p = perms();
        assert_eq!(p.role_of("admin-uid"), "admin");
        assert_eq!(p.role_of("stranger"), "guest");
    }
}
```

- [ ] **Step 3: members**
根 `Cargo.toml` members 追加 `crates/perms`（置于 playlist 之后）：
```toml
members = ["crates/config", "crates/audio", "crates/ts-connection", "crates/source", "crates/playlist", "crates/perms", "crates/player", "crates/commands", "crates/tsbot"]
```

- [ ] **Step 4: 测试** `cargo test -p tsbot-perms 2>&1 | grep "test result"` → 5 pass。

- [ ] **Step 5: Commit** `git add -A && git commit -m "feat(perms): 角色+指令权限位 Permissions（opt-in）"`

---

## Task 2: ChatMessage 增加 invoker_uid

**Files:** Modify `crates/ts-connection/src/lib.rs`, `crates/commands/src/lib.rs`

- [ ] **Step 1: ts-connection ChatMessage 加字段**
把 `ChatMessage` 改为：
```rust
/// 收到的频道文本消息，driver 转发给指令处理器。
pub struct ChatMessage {
    pub text: String,
    pub invoker_id: ClientId,
    pub invoker_uid: String,
}
```

- [ ] **Step 2: driver 填充 invoker_uid**
在 `run` 的事件闭包里，构造 `ChatMessage` 处（`Event::Message { target: MessageTarget::Channel, invoker, message }` 分支）改为：
```rust
                        if invoker.id != own_id {
                            let invoker_uid =
                                invoker.uid.as_ref().map(|u| u.to_string()).unwrap_or_default();
                            let _ = chat_tx.try_send(ChatMessage {
                                text: message.clone(),
                                invoker_id: invoker.id,
                                invoker_uid,
                            });
                        }
```
（`invoker.uid: Option<UidBuf>`；`u.to_string()` 经 Deref→`Uid: Display` 得 base64 uid 串。若 `u.to_string()` 不编译，用 `(&**u).to_string()`。）

- [ ] **Step 3: commands 测试构造补 invoker_uid**
`crates/commands/src/lib.rs` 测试里所有 `ChatMessage { text: ..., invoker_id: ts_connection::ClientId(1) }` 构造，补 `invoker_uid: "u1".into()`：
```rust
ChatMessage { text: t.into(), invoker_id: ts_connection::ClientId(1), invoker_uid: "u1".into() }
```
（`run_volume_loop_remove_replies` 测试里的 `send` 闭包构造处。playlist 测试不构造 ChatMessage，不用改。）

- [ ] **Step 4: 构建 + 全测试**
Run: `cargo build --workspace 2>&1 | tail -5 && cargo test --workspace 2>&1 | grep -E "test result" | grep -oE "[0-9]+ passed" | awk '{s+=$1} END {print s" passed"}'`
Expected: 编译成功；全绿（perms 5 + 之前 29 = 34）。

- [ ] **Step 5: Commit** `git add -A && git commit -m "feat(ts-connection): ChatMessage 增加 invoker_uid（base64 uid）"`

---

## Task 3: config 加 [permissions]

**Files:** Modify `crates/config/Cargo.toml`, `crates/config/src/lib.rs`, `config.example.toml`

- [ ] **Step 1: config 依赖 perms**
`crates/config/Cargo.toml` `[dependencies]` 加：`tsbot-perms = { path = "../perms" }`

- [ ] **Step 2: Config 加 permissions 字段**
`crates/config/src/lib.rs`：顶部 `use` 后无需特别引入（用全路径）。`Config` 改为：
```rust
#[derive(Debug, Deserialize)]
pub struct Config {
    pub server: Server,
    pub bot: Bot,
    #[serde(default)]
    pub playlist: PlaylistCfg,
    #[serde(default)]
    pub permissions: perms::Permissions,
}
```
（`Server`/`Bot`/`PlaylistCfg`/`load` 不变。`perms::Permissions` 来自 tsbot-perms（lib 名 `perms`）。）

- [ ] **Step 3: 加测试**
config 测试模块追加：
```rust
    #[test]
    fn permissions_default_empty_means_disabled() {
        let (_d, p) = write_tmp(
            "[server]\naddress = \"x\"\n[bot]\nname = \"b\"\nidentity_path = \"i\"\n",
        );
        let c = load(&p).unwrap();
        assert!(c.permissions.roles.is_empty());
        assert!(c.permissions.allows("anyone", "stop")); // 空 = 全开
    }
```

- [ ] **Step 4: config.example.toml 追加 [permissions] 示例**
```toml

# 权限（可选）：删除整个 [permissions] 段 = 关闭权限（任何人可用所有命令）
[permissions]
default_role = "guest"          # 陌生用户角色

[permissions.roles]
admin = ["*"]
dj    = ["play", "skip", "pause", "resume", "volume", "loop", "shuffle", "remove", "clear", "stop", "playlist"]
guest = ["play", "queue", "nowplaying"]

[permissions.users]
# "你的uid（用 !whoami 获取）" = "admin"
```

- [ ] **Step 5: 测试** `cargo test -p tsbot-config 2>&1 | grep "test result"` → 4 pass（原 3 + 新 1）。`cargo build --workspace 2>&1 | tail -3`。

- [ ] **Step 6: Commit** `git add -A && git commit -m "feat(config): 增加 [permissions]（serde default 兼容旧配置）"`

---

## Task 4: commands 门禁 + !whoami + bin 接线

**Files:** Modify `crates/commands/Cargo.toml`, `crates/commands/src/lib.rs`, `crates/tsbot/Cargo.toml`, `crates/tsbot/src/main.rs`

- [ ] **Step 1: commands 依赖 perms**
`crates/commands/Cargo.toml` `[dependencies]` 加：`tsbot-perms = { path = "../perms" }`

- [ ] **Step 2: imports + Command::Whoami + parse**
顶部 `use` 加：`use perms::Permissions;`
`Command` 枚举加变体：`    Whoami,`
`parse` 的 match 加（`_ => None` 之前）：`        "whoami" => Some(Command::Whoami),`
`HELP_TEXT` 末尾追加一行（在最后字符串里）：把现有结尾改为以
```
!whoami 查看你的 uid";
```
结束（即在原 `!playlist ...` 行后加 `\n\` 续行再加 whoami 行）。

- [ ] **Step 3: command_name 映射函数**
在 `parse` 之后新增：
```rust
/// 指令 → 权限键名。
fn command_name(cmd: &Command) -> &'static str {
    match cmd {
        Command::Play(_) => "play",
        Command::Skip => "skip",
        Command::Stop => "stop",
        Command::Pause => "pause",
        Command::Resume => "resume",
        Command::NowPlaying => "nowplaying",
        Command::Queue => "queue",
        Command::Clear => "clear",
        Command::Shuffle => "shuffle",
        Command::Help => "help",
        Command::Whoami => "whoami",
        Command::Volume(_) => "volume",
        Command::Loop(_) => "loop",
        Command::Remove(_) => "remove",
        Command::Playlist(_) => "playlist",
    }
}
```

- [ ] **Step 4: run 加 perms 参数 + 门禁；handle_command 加 uid + Whoami 臂**
`run` 改为：
```rust
pub async fn run(
    mut chat_rx: mpsc::Receiver<ChatMessage>,
    handle: PlayerHandle,
    snapshot: Arc<Mutex<Snapshot>>,
    store: Store,
    perms: Permissions,
    reply_tx: mpsc::Sender<String>,
) {
    while let Some(msg) = chat_rx.recv().await {
        let Some(cmd) = parse(&msg.text) else { continue };
        let name = command_name(&cmd);
        if name != "help" && name != "whoami" && !perms.allows(&msg.invoker_uid, name) {
            let _ = reply_tx
                .send(format!("⛔ 无权限：{name}（你的角色：{}）", perms.role_of(&msg.invoker_uid)))
                .await;
            continue;
        }
        let reply = handle_command(cmd, &handle, &snapshot, &store, &msg.invoker_uid).await;
        let _ = reply_tx.send(reply).await;
    }
}
```
`handle_command` 签名加 `uid: &str`：
```rust
async fn handle_command(cmd: Command, handle: &PlayerHandle, snapshot: &Arc<Mutex<Snapshot>>, store: &Store, uid: &str) -> String {
```
并在其 match 加一臂（`Command::Playlist` 之后）：
```rust
        Command::Whoami => format!("你的 uid：[b]{uid}[/b]"),
```

- [ ] **Step 5: 更新现有 run 测试 + 加门禁/映射测试**
`crates/commands/src/lib.rs` 测试模块：
(a) `run_volume_loop_remove_replies` 与（若有）其它调用 `run(...)` 处，增加 `Permissions::default()` 参数（默认空 = 全开，保持原行为）。例如：
```rust
        let join = tokio::spawn(run(chat_rx, handle, snap, store, perms::Permissions::default(), reply_tx));
```
（同时该测试的 `ChatMessage` 构造已在 Task 2 补了 `invoker_uid`。）
(b) 追加：
```rust
    #[test]
    fn command_name_maps() {
        assert_eq!(command_name(&Command::Play(vec![])), "play");
        assert_eq!(command_name(&Command::Whoami), "whoami");
        assert_eq!(command_name(&Command::Playlist(vec![])), "playlist");
        assert_eq!(command_name(&Command::Stop), "stop");
    }

    #[tokio::test]
    async fn run_enforces_permissions() {
        use std::collections::HashMap;
        let dir = tempfile::tempdir().unwrap();
        let store = Store::new(dir.path().to_path_buf());
        let (player, handle, snap) = player::Player::new().unwrap();
        drop(player);
        let mut roles = HashMap::new();
        roles.insert("guest".to_string(), vec!["queue".to_string()]);
        let perms = Permissions { default_role: "guest".to_string(), roles, users: HashMap::new() };
        let (chat_tx, chat_rx) = mpsc::channel(8);
        let (reply_tx, mut reply_rx) = mpsc::channel(8);
        let join = tokio::spawn(run(chat_rx, handle, snap, store, perms, reply_tx));
        let send = |t: &str| chat_tx.send(ChatMessage {
            text: t.into(),
            invoker_id: ts_connection::ClientId(1),
            invoker_uid: "u1".into(),
        });

        send("!stop").await.unwrap();
        assert!(reply_rx.recv().await.unwrap().contains("无权限"));
        send("!queue").await.unwrap();
        assert!(reply_rx.recv().await.unwrap().contains("没有播放")); // 放行
        send("!help").await.unwrap();
        assert!(reply_rx.recv().await.unwrap().contains("!play")); // 永远放行
        send("!whoami").await.unwrap();
        assert!(reply_rx.recv().await.unwrap().contains("u1")); // 回 uid

        drop(chat_tx);
        let _ = join.await;
    }
```

- [ ] **Step 6: bin 接线**
`crates/tsbot/Cargo.toml` `[dependencies]` 加：`tsbot-perms = { path = "../perms" }`
`crates/tsbot/src/main.rs`：把 `commands::run` 调用加 `config.permissions`：
```rust
    tokio::spawn(commands::run(chat_rx, handle, snapshot, store, config.permissions, reply_tx));
```
（注意 `config` 已含 `permissions` 字段；其余不变。`config` 在此前已被 move 取用了 `server/bot/playlist/identity_path` 等——确认 `config.permissions` 的取用不与已 move 的字段冲突：当前 main 是逐字段读取 config 的，`config.permissions` 在最后取用即可。若 `config` 整体已被部分移动导致借用错误，调整为在构造 settings 前先 `let permissions = config.permissions;` 暂存。）

- [ ] **Step 7: 构建 + 全测试 + clippy**
Run: `cargo build --workspace 2>&1 | tail -6 && cargo test --workspace 2>&1 | grep -E "test result" | grep -oE "[0-9]+ passed" | awk '{s+=$1} END {print s" passed"}' && cargo clippy --workspace 2>&1 | grep -ciE "warning|error"`
Expected: 编译成功；全绿；clippy 0。

- [ ] **Step 8: Commit** `git add -A && git commit -m "feat(commands): 权限门禁 + !whoami + bin 接线"`

---

## Task 5: 全量校验与人工验收

**Files:** 无代码改动

- [ ] **Step 1: 全量构建/测试/lint**
Run: `cargo build --workspace 2>&1 | tail -3 && cargo test --workspace 2>&1 | tail -3 && cargo clippy --workspace 2>&1 | grep -ciE "warning|error"`
Expected: 全绿；clippy 0。

- [ ] **Step 2: 人工验收**
1. **不配 `[permissions]`**：跑起来，任意用户所有命令可用（权限关闭，兼容现状）。
2. 在 `config.toml` 配 admin/dj/guest 角色；频道发 `!whoami` 拿到 uid → 填入 `[permissions.users] "uid" = "admin"` → 重启。
3. 管理员：全部命令可用。
4. 另一未列出用户：`!play`/`!queue`/`!nowplaying` 可用；`!stop`/`!volume`/`!playlist save` 被拒，回 "⛔ 无权限：…（你的角色：guest）"。
5. `!help`/`!whoami` 对任何人都可用。

- [ ] **Step 3: 记录结果**
全流程符合即 Phase 3 通过。失败排查：uid 不匹配→`!whoami` 输出与配置 key 比对（base64 完整串）；权限没生效→确认重启加载了新配置、`[permissions]` 段存在使 roles 非空。

---

## Self-Review 记录
- **Spec 覆盖**：perms 库+allows/role_of+opt-in(Task1)、ChatMessage uid(Task2)、config [permissions](Task3)、Whoami+command_name+run 门禁+bin(Task4)、人工验收(Task5)。逐条对应。
- **占位符扫描**：无 TODO/TBD；每步含完整代码与命令。
- **类型一致性**：`Permissions{default_role,roles,users}`+`allows`/`role_of`、`ChatMessage{text,invoker_id,invoker_uid}`、`Command::Whoami`、`command_name`、`run(...,perms,...)`、`handle_command(...,uid)`、`config.permissions` 跨任务一致。
- **依赖无环**：perms 仅依赖 serde；config→perms、commands→perms、bin→{config,commands,...,perms}；无环。
- **绿色保持**：Task2 补 ChatMessage 构造、Task4 补 run 参数——每步可编译可测。
- **风险**：`UidBuf.to_string()` 转换（带 `(&**u).to_string()` 兜底）；bin 中 `config.permissions` 的 move 顺序（带暂存兜底）。
