[package]
name = "tsbot-source"
version = "0.1.0"
edition = "2021"

[lib]
name = "source"

[dependencies]
tokio = { workspace = true, features = ["process", "macros", "rt", "time"] }
anyhow = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
